# DeepL API の認証キー（サーバー側でのみ使用。NEXT_PUBLIC_ は付けないこと）
# 無料版 (DeepL API Free) のキーは末尾が ":fx" で、api-free ホストを使います。
# 取得: https://www.deepl.com/ja/pro-api → 無料登録 → アカウント → 認証キー
# 未設定でもアプリは「モック翻訳」で動作します（デモ用）。
DEEPL_API_KEY=

# 任意: Pro 版を使う場合などにエンドポイントを上書き
# DEEPL_API_URL=https://api.deepl.com/v2/translate
